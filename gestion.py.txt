import os

# Esto busca la ruta absoluta de donde está tu código
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
ARCHIVO_EXCEL = os.path.join(BASE_DIR, 'base_datos.xlsx')

# Ahora, cuando cargues el archivo, usa esa variable
def cargar_datos():
    if os.path.exists(ARCHIVO_EXCEL):
        return pd.read_excel(ARCHIVO_EXCEL)
    else:
        st.error(f"No se encontró el archivo en: {ARCHIVO_EXCEL}")
        return None
